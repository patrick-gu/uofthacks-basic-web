{
  "devDependencies": {
    "@types/node": "^20.11.7",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "build": "tsc -p .",
    "compile": "npm run build && node build/compiler/src/parser.js"
  }
}
